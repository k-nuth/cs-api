<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
	<Target Name="CustomScriptTest" AfterTargets="ResolveReferences" BeforeTargets="FindReferenceAssembliesForReferences">
		<Message Text="Custom build step called for .NET 4.6.1!" />
    <Exec Command="python --version"/>
    <Exec Command="conan remote add bitprim https://api.bintray.com/conan/bitprim/bitprim"/>
    <Exec Command="conan install bitprim-node-cint/0.3@bitprim/testing -o shared=True"/>
	</Target>
</Project>